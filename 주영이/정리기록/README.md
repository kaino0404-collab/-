# 사진·영상 정리 기록 (2026-09-23 ~ 09-24)

`요딩이` 폴더에서 아기(주영이)가 나오는 사진·영상을 `주영이` 폴더로 옮긴 기록입니다.
사진·영상 파일은 개인정보와 용량 문제로 이 저장소에 올리지 않았고, 파일 이름 목록만 남깁니다.

## 결과
| | 사진 | 영상 | 합계 |
|---|---|---|---|
| 주영이로 이동 | 1,126 | 521 | 1,647 |
| 요딩이에 남김 | 240 | 22 | 262 |

- 주영이 폴더: 887개 → 2,534개
- 중복 제거: SHA-256 해시로 비교했을 때 완전히 같은 파일도, 이름이 같은 파일도 없어 삭제한 파일은 없음
- 남긴 파일: 출산 전 임신 기간 사진·영상, 음식, 풍경, 빈 병실, 아기가 안 나오는 영상 등

## 파일
- `이동한_파일.txt` — 주영이 폴더로 옮긴 파일 이름 1,647개
- `남긴_파일.txt` — 요딩이 폴더에 그대로 둔 파일 이름 262개
- `scripts/`
  - `sheets.py` — 사진 9장, 영상은 3개(각 20%·50%·80% 지점 프레임)씩 번호 붙인 확인용 시트로 만듦
  - `mover.py` — 시트별 판단 기록(`decisions.txt`)대로 파일 이동 (덮어쓰기 없음)
  - `recheck.py` — 애매한 영상은 프레임 12장을 뽑아 다시 확인
  - `progress.py` — 중단된 이전 작업에서 이미 판단된 파일 계산
